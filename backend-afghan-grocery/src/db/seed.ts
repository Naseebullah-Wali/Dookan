// Database seeding script removed in migration to Supabase.
// Use the SQL file at afghan-grocery-vue/supabase-add-news-testimonials.sql
// or run seeding via the Supabase dashboard or scripts that use the Supabase client.
console.error('Seeding script deprecated. Seed data using Supabase SQL or the dashboard.');
export const seedDatabase = async (): Promise<void> => {
    throw new Error('Seeding is deprecated. Use Supabase.');
};

export default seedDatabase;
