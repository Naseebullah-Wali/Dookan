// reCAPTCHA has been removed; keep a stub to avoid runtime errors if imported
export class RecaptchaService {
    static async verify(): Promise<boolean> {
        return true
    }
}

export default RecaptchaService
